let gameName = "GTA VI"
gameName = "Forza Horizon 5"
console.log(gameName);

// let keyword can be used when we want to change the value of a variable

const userName = "sarthak10"
userName = "sarthak08"
// console.log(userName);

// Const keyword doesn't allow the value of variable to be changed once initialized
